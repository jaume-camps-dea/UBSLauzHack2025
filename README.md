# Data Insights & Fraud Detection Platform

A powerful, containerized web application that transforms your data into actionable intelligence. This platform provides automated daily briefings, a conversational AI assistant for deep data exploration, and integrated visual tools for fraud analysis.

## Overview
<img width="1356" height="681" alt="image" src="https://github.com/user-attachments/assets/08cd6326-8fe9-4452-b46e-aa48961391c2" />

## 🚀 Features

### 📊 Daily Briefing Dashboard
Get an immediate pulse on your key metrics. The dashboard automatically surfaces:
- **Weekly Performance:** Track critical KPIs and trends over the past week.
- **Historical Analysis:** View long-term data patterns and comparisons to identify significant changes and opportunities.

### 🤖 Conversational AI Data Assistant
Go beyond predefined reports. Ask questions in plain English and get intelligent answers.
- **Natural Language to SQL:** Our LangChain-powered AI agent understands your intent, translates your question into an optimized SQL query, and executes it directly on your database.
- **Intelligent Response Generation:** The raw query results are sent to another LLM to generate a clear, human-readable summary, highlighting the most important data points relevant to your query.
- **User-Centric Deep Dives:** When your conversation pertains to a specific user, the system automatically generates relevant KPIs and visualizations for that entity.

### 🔍 Integrated Fraud Detection
- **Visual Fraud Analysis:** The user-specific visualizations generated by the AI assistant are designed to empower analysts to spot anomalies and potential fraudulent activities quickly.
- **Proactive Monitoring:** By making individual user data easily accessible and visual, the platform facilitates rapid investigation and risk assessment.

## 🛠️ Tech Stack

| Component | Technology |
|-----------|------------|
| **Backend & API** | Python, Flask |
| **AI & NLP Engine** | LangChain, OpenAI / Gemini / Local LLM |
| **Frontend** | HTML + CSS |
| **Database** | **Google BigQuery** |
| **Deployment** | **Docker**, Docker Compose |
| **Visualization** | Plotly, Matplotlib, Chart.js  |

## 📋 Prerequisites

Before you begin, ensure you have the following installed:
- [Docker](https://www.docker.com/get-started)
- [Docker Compose](https://docs.docker.com/compose/install/)
- A Google Cloud Project with BigQuery API enabled and a service account key (JSON file).

## 🏁 Quick Start

Get the project up and running on your local machine in a few simple steps.

1.  **Clone the Repository**
    ```bash
    git clone https://github.com/your-username/your-data-platform.git
    cd your-data-platform
    ```

2.  **Configure Environment Variables**
    Create a `.env` file in the root directory by copying the example and filling in your credentials.
    ```bash
    cp .env.example .env
    ```
    Edit the `.env` file:
    ```env
    # BigQuery Configuration
    GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
    BIGQUERY_PROJECT_ID=your-gcp-project-id
    BIGQUERY_DATASET=your_bigquery_dataset

    # LLM Configuration (e.g., for OpenAI)
    OPENAI_API_KEY=your-openai-api-key

    # Application Settings
    SECRET_KEY=your-secret-key-here
    ```

3.  **Build and Run with Docker**
    This command builds the images and starts all services (web server, backend, etc.).
    ```bash
    docker-compose up --build
    ```

4.  **Access the Application**
    Open your web browser and navigate to `http://localhost:8501` (if using Streamlit) or `http://localhost:3000` (if using React) to see the application in action!

## 🗂️ Project Structure

```
.
data-insights-platform/
├── docker-compose.yml
├── Dockerfile
├── .env.example
├── requirements.txt
├── app/
│ ├── init.py
│ ├── main.py # FastAPI application entry point
│ ├── agents/ # LangChain agent logic
│ │ ├── query_generator.py # Converts natural language to SQL
│ │ └── response_builder.py # Generates nice answers from query results
│ ├── services/
│ │ ├── bigquery_client.py # Handles all DB interactions
│ │ └── briefing_service.py # Fetches data for the daily briefing
│ ├── static/ # CSS, JS, Images
│ ├── templates/ # HTML templates
│ └── utils/
│ └── visualization.py # Helper functions for generating charts
└── frontend/ 
├── public/
├── src/
│ ├── components/
│ ├── pages/
│ └── App.js
```


## 💬 Using the AI Assistant

1.  Navigate to the "AI Assistant" tab in the web interface.
2.  Type your question. For example:
    - *"What were the total sales for the top 5 products last month?"*
    - *"Show me the login activity for user 'john_doe_123' over the last 90 days."*
3.  The agent will:
    - Understand the context of your question.
    - Generate and run a query on BigQuery.
    - Return a concise summary with key figures.
    - If relevant, automatically display charts related to the user in question for visual analysis.

## 🔧 Development

To contribute to the project, you can run it outside of Docker for a easier development workflow.

1.  **Create a Python Virtual Environment**
    ```bash
    python -m venv venv
    source venv/bin/activate  # On Windows: `venv\Scripts\activate`
    ```

2.  **Install Dependencies**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Run the Application**
    ```bash
    uvicorn app.main:app --reload  # For FastAPI backend
    streamlit run app/main.py      # If using Streamlit for frontend
    ```

## 📈 Future Enhancements

- [ ] Automated alerting system for detected anomalies.
- [ ] User authentication and role-based access control (RBAC).
- [ ] Pre-defined report scheduling and email delivery.
- [ ] Support for additional data sources (e.g., PostgreSQL, Snowflake).

## 👥 Contributing

Jaume Camps Romaguera
Marcel Fernández Serrano
Marti Oms Graells
Bruno Manzano Clotet
---

**Happy Analyzing!** 🎉
